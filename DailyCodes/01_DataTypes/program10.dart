/* constant : when we declier a variable constant we can't change it further

we can declier variable constant by two ways 
1. const = compail time costatnt
2. final = runtime constant

*/


void main () {

	const int y = 10;
	const x = 20.5;  /*we can also declier by this way by don't use this ,not a
			   good programing */

	print(y);
	print(x);

	// x = 30.5;   Error : can"t assign to the constant variable
	
	//const int z ;
	//z = 10;    Error
}
		

